apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pihole-route
  namespace: homeserver
spec:
  parentRefs:
  - name: homeserver

  hostnames:
  - pihole.homeserver.local

  rules:
  - backendRefs:
    - name: pihole-svc
      port: 80