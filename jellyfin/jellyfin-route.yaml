apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jellyfin-route
  namespace: homeserver

spec:
  parentRefs:
  - name: homeserver

  hostnames:
  - jellyfin.homeserver.local

  rules:
  - backendRefs:
    - name: jellyfin-svc
      port: 8096
      